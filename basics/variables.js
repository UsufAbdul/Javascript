//1. You cant define a variable more than once
let petName = "Hal";

petName = "Spot"; // it prints this
console.log(petName);

// 2. there are rules related to variable names (dont use numbers to start a variable name)
let test_ = 2;
let results = 3 + 4;

//3. variable names cannot be reserved keywords
// let let = 12
